fn main() {
    // TODO: Change the line below to fix the compiler error.
    /// 变量 `x` 被初始化为 `i32` 类型的值 `42`。
    /// 
    /// 在 Rust 中，变量可以显式地指定类型，例如 `let x: i32 = 42;`，
    /// 也可以通过类型推断自动确定类型，例如 `let x = 42;`。
    /// 
    /// Rust 中的基本类型包括：
    /// - 整数类型（`i8`, `i16`, `i32`, `i64`, `i128`, `isize`）
    /// - 无符号整数类型（`u8`, `u16`, `u32`, `u64`, `u128`, `usize`）
    /// - 浮点数类型（`f32`, `f64`）
    /// - 布尔类型（`bool`）
    /// - 字符类型（`char`）
    /// 
    /// 另外，Rust 还有一些复合类型，例如：
    //— 元组（`tuple`）
    /// - 数组（`array`）
    /// - 切片（`slice`）
    /// - 字符串切片（`&str`）
    let x: i32 = 42; // 初始化变量 x

    println!("Number {x}");
}
